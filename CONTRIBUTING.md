# 协作方式

## 修改已有项目

从最新 main 建立自己的分支。建议分支名包含课程和工作内容，例如 `bn5212/add-training`、`bn5212/update-evaluation`。每次 Pull Request 尽量解决一个明确问题，说明改了什么、如何运行、做了什么验证。

不直接覆盖他人的模块。涉及 Dataset 返回值、index 字段、标签定义或患者划分的修改，应在 Pull Request 中说明影响，并请数据与建模成员一起查看。数据接口版本变化时同时更新 `docs/DATALOADER_SPEC.md`。

## BN5212 成员交接

| 工作内容 | 交接要求 |
|---|---|
| 数据处理 | 提供运行配置、清洗统计、冻结的患者划分、接口版本 |
| 模型训练 | 使用 `make_dataloader`，记录采用的 index 和 split 版本 |
| 模型评估 | 共同使用冻结测试集，事先说明按图像、住院或患者聚合 |
| 报告与汇报 | 说明真实样本数量来源，区分合成测试和真实研究结果 |

真实数据衍生文件通过课程允许的共享位置交接，不提交至此 Git 仓库。提交代码时不要包含本地密码、下载令牌、受限数据或私人配置。现有 `.gitignore` 为常用本地文件提供排除规则，提交前仍应查看 GitHub 的文件变更列表。

## 加入其他课程

1. 新建 `courses/<课程代码>/<项目名称>/`。
2. 用 `templates/project-README.md` 描述项目内容、依赖、输入输出与运行方式。
3. 将该项目的临时输出、环境和私人配置加入项目自己的 `.gitignore`。
4. 在根 README 添加项目入口。需要自动检查时，为该课程新增独立工作流，并限定触发路径。

命名忽略目录时注意作用范围，例如项目根数据目录写成 `/data/`，以免错误忽略 `src/data/` 源码目录。

## 合并与仓库权限

先让改动的作者运行必要测试，再由组员查看 Pull Request 后合并。仓库所有者负责邀请或移除协作者；增加权限或修改仓库可见性由所有者决定。本仓库文档中的协作规则是团队约定，不代表已经在 GitHub 配置了分支保护或邀请了组员。
