# 人工索引示例

`synthetic_index.csv` 是测试生成器输出中的 6 行摘录，覆盖三个 split 的两个标签，展示统一索引列与格式。

这些是**完全人工构造**的患者、住院与图像标识，不含真实 MIMIC 数据；也不是完整可加载的数据集。对应人工图像不随此摘录分发。要获得可运行的完整人工 dataset，请按项目 README 运行 `scripts/make_synthetic_data.py` 和流水线。
